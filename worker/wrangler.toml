name = "pcmsomagede-cms-api"
main = "src/index.ts"
compatibility_date = "2026-08-17"

[vars]
GITHUB_OWNER = "pcmsomagede"
GITHUB_REPO = "pcmsomagede"
PUBLIC_SITE_ORIGIN = "https://pcmsomagede.github.io"

# Secret values are managed in the Cloudflare Worker dashboard.
# GITHUB_TOKEN, ADMIN_USER, ADMIN_PASSWORD, and SESSION_SECRET remain Worker secrets.
# CMS API deployment
